#include <stdio.h>

int main(){

int minutes, hours;

printf("enter the numbre of minutes is : ");
scanf("%d",& minutes);

minutes / 60;
printf("the number of minutes into  %d hours ",minutes / 60 );

 return 0;
}