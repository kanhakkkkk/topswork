#include <stdio.h>

int main(){

//kilomiters 1 = 1000 miters
float kilomiters, miter;

printf("enter the number of kilometers is: ");
scanf("%f",&kilomiters);

kilomiters * 1000;
printf("the number of kilomiter is  %f miters", kilomiters * 1000);


    return 0;
}