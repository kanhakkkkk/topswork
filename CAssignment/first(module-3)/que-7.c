#include <stdio.h>

int main(){
   //the area of rectangle
    float area, length, width;

    printf(" the length  of rectngle");
    scanf("%f",&length);

    printf(" the width  of rectngle");
    scanf("%f",&width);

    area = length*width;

    printf("the area of rectngle is %f", area);


    return 0;
}